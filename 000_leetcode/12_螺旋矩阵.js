/**
 * 螺旋矩阵：
 *  四个方向： 右 ->  下 -> 左 -> 上
 *  四个边界：let, right, top, bottom
 */
function rota(arr) {

    let top = 0
    let bottom = arr.length - 1
    let left = 0
    let right = arr[0].length - 1
    let direction = 'right'
    let result = []

    while(left < right && top <= bottom) {

    }
}